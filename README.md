# RIO - Raw IO benchmark uitility

RIO will read/write a single file.

It can do it in multiple threads, in a random or in a sequenced way.

## Compile

```shell
$ make
````

## Usage

```shell
$ ./rio
```
